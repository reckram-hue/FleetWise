rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // DEV ONLY: allow everything for now (mock UI has no auth)
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
