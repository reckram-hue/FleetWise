{
  "version": 2,
  "builds": [
    { "src": "index.html", "use": "@vercel/static-build", "config": { "distDir": "dist" } }
  ],
  "routes": [
    { "src": "^/health$", "dest": "http://localhost:5174/health" },
    { "src": "^(?!/health).*$", "dest": "/" }
  ]
}
